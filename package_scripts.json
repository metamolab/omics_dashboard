{
  "scripts": {
    "start": "ng serve",
    "start:api": "python fastapi_main.py",
    "start:dev": "concurrently \"npm run start:api\" \"npm run start\"",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  }
}
